---
description: Initialize SODA pipeline for current repo
---

Initialize a SODA pipeline configuration for the current repository:

```bash
soda init $ARGUMENTS
```

This sets up the initial `soda.yaml` and `phases.yaml` configuration files for the project.
