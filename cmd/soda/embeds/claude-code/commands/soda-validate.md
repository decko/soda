---
description: Validate SODA configuration without running
---

Check config, phases, and prompts for errors without running a pipeline:

```bash
soda validate
```

Validates:
- `soda.yaml` configuration file
- `phases.yaml` pipeline definitions
- Prompt template syntax and resolution
- Schema file references
